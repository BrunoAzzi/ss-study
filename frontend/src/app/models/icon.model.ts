export class Icon {
    id: number;
    name: string;
    size: [number, number];

    constructor(name: string, size: [number, number]) {
        this.name = name;
        this.size = size;
    }


}